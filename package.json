{
  "name": "pyuseref",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "NODE_ENV=development parcel --log-level info src/index.html --dist-dir dist/dev",
    "build": "NODE_ENV=production parcel build --log-level info --no-cache --no-source-maps src/index.html --dist-dir dist/prod"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/JennaSys/pyuseref.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "react": "^16.14.0",
    "react-dom": "^16.14.0"
  },
  "devDependencies": {
    "parcel": "^2.2.1",
    "parcel-transformer-transcrypt": "^0.1.3"
  }
}
