{
  "name": "pyuseref",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "NODE_ENV=development parcel --no-cache --log-level 4 src/index.html --out-dir dist/dev",
    "build": "NODE_ENV=production parcel --no-cache --log-level 4 build src/index.html --no-source-maps --out-dir dist/prod",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/JennaSys/pyuseref.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "parcel-bundler": "^1.12.4",
    "parcel-plugin-transcrypt": "^1.0.20"
  },
  "dependencies": {
    "react": "^16.14.0",
    "react-dom": "^16.14.0"
  }
}
